<Window x:Class="CapturePanel.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Capture Panel"
        Width="560"
        Height="528"
        MinWidth="560"
        MinHeight="528"
        MaxWidth="560"
        MaxHeight="528"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid Background="{DynamicResource ApplicationBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0"
                       Width="40"
                       Height="40"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Source="/CapturePanel;component/Assets/CapturePanel.png"
                       AutomationProperties.Name="Capture Panel icon" />
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource PageTitleStyle}" Text="Capture Panel" />
                    <TextBlock Margin="0,2,0,0"
                               Style="{StaticResource SecondaryTextStyle}"
                               Text="{Binding VersionText}" />
                </StackPanel>
            </Grid>

            <Border Margin="0,20,0,12" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Native audio worker" />
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Ellipse Grid.Column="0"
                                 Width="8"
                                 Height="8"
                                 Margin="0,0,9,0"
                                 VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WorkerAvailable}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource SystemFillColorSuccessBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Grid.Column="1"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"
                                   Text="{Binding WorkerStatusText}" />
                    </Grid>
                    <TextBox Margin="0,10,0,0"
                             Padding="9,6"
                             IsReadOnly="True"
                             FontFamily="Cascadia Mono, Consolas"
                             FontSize="11"
                             Text="{Binding WorkerPath, Mode=OneWay}" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Licenses and notices" />
                    <TextBlock Margin="0,5,0,14"
                               Style="{StaticResource SecondaryTextStyle}"
                               TextWrapping="Wrap"
                               Text="Capture Panel is licensed under GNU GPL version 3 only. ASIO interface files and the self-contained .NET runtime retain their own notices." />

                    <Button Margin="0,0,0,8"
                            Padding="12,8"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            Command="{Binding OpenLicenseCommand}"
                            AutomationProperties.Name="Open GPL version 3 license">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="GNU GPL v3 License" />
                            <TextBlock Grid.Column="1"
                                       Margin="12,0,0,0"
                                       FontFamily="Segoe Fluent Icons"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       Text="&#xE8A7;" />
                        </Grid>
                    </Button>

                    <Button Margin="0,0,0,8"
                            Padding="12,8"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            Command="{Binding OpenThirdPartyNoticesCommand}"
                            AutomationProperties.Name="Open third-party notices">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Third-Party Notices" />
                            <TextBlock Grid.Column="1"
                                       Margin="12,0,0,0"
                                       FontFamily="Segoe Fluent Icons"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       Text="&#xE8A7;" />
                        </Grid>
                    </Button>

                    <Button Padding="12,8"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            Command="{Binding OpenAsioLicenseCommand}"
                            AutomationProperties.Name="Open Steinberg ASIO SDK license">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Steinberg ASIO SDK License" />
                            <TextBlock Grid.Column="1"
                                       Margin="12,0,0,0"
                                       FontFamily="Segoe Fluent Icons"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       Text="&#xE8A7;" />
                        </Grid>
                    </Button>
                </StackPanel>
            </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
